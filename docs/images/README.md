# Performance Evidence

The performance screenshot used in the documentation lives in this folder:

- `performance.png` — Captured from Chrome DevTools (Performance tab) showing LCP ~0.28s and CLS ~0.03 on a local run with no throttling.
